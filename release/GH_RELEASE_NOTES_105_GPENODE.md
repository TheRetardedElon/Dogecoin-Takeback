# GPENode 1.14.105

Same Windows installer as [Dogecoin-Takeback v1.14.105](https://github.com/TheRetardedElon/Dogecoin-Takeback/releases/tag/v1.14.105). One `dogecoind`. Pick **Server** or **Hybrid**.

- Operator TUI: `gpenode-tui --testnet` is green (TESTNET header). Server Start Menu: **Dogecoin Operator TUI Testnet**
- Testnet does not start/stop the mainnet `DogecoinGPENode` service
- Linux apt / `.deb` follow in a separate push
